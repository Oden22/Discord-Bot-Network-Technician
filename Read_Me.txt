Requirements
1.	Pip Install the requirements.txt
2.	If using virtual env open venv/ Lib / sitepackages otherwise just search and change Speedtest.py line 186 to:
	connection = self.connect('c.speedtest.net')
3.	Install wireshark from https://www.wireshark.org/#download (you only need the tshark and npcap for this project)
